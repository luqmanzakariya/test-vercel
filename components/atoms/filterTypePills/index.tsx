const FilterTypePills = ({ text }: { text: string }) => {
  return (
    <button className="rounded-full border border-gray-700 font-abril py-2 px-5">{text}</button>
  );
};

export default FilterTypePills;
